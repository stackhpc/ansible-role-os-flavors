---
galaxy_info:
  #role_name: os_flavors
  author: Mark Goddard
  description: >
    Role to register nova flavors in OpenStack
  company: StackHPC Ltd
  license: Apache2
  min_ansible_version: 2.9
  platforms:
    - name: EL
      versions:
        - 7
  galaxy_tags:
    - cloud
    - keystone
    - openstack

dependencies:
  - role: stackhpc.os_openstacksdk
    os_openstacksdk_venv: "{{ os_flavors_venv }}"
